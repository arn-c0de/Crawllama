{
  "schema_version": 1,
  "id": "capabilities",
  "description": "Milestone B capability drivers that construct the real production agents offline via an injected fake LLM (SearchAgent context-only, MultiHopReasoningAgent). Deterministic and network-free, but heavier than the smoke suite.",
  "scenarios": [
    "agent.context_only.v1",
    "multihop.reasoning.v1"
  ]
}
