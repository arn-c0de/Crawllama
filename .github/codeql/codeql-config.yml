name: "CodeQL Config"

# Use default security and quality queries
queries:
  - uses: security-and-quality

# Exclude test files from security scans
# Tests often log test data that looks like sensitive info but isn't
paths-ignore:
  - tests/**
  - docs/**
  - scripts/**
  - examples/**
  - "*.md"
  - "*.txt"
