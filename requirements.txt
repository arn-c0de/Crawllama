# last updated: 2026-03-02 (dependency graph refresh trigger)
# ===== CORE (Always installed) =====
requests>=2.32.5
beautifulsoup4>=4.14.3
html5lib>=1.1
urllib3>=2.6.3  # fix decompression bomb CVE-2025-69223 (Dependabot alert #32); if Brotli present, pin >=1.2.0
ddgs>=9.11.4
chromadb>=1.5.5
pyasn1>=0.6.3 
tenacity>=9.1.4
rich>=14.3.3
python-dotenv>=1.2.2
cryptography>=46.0.5  # fix SECT curve subgroup attack CVE-2026-26007 (Dependabot alert #39)
redis>=7.3.0
fakeredis>=2.34.1
wikipedia>=1.4.0
tiktoken>=0.12.0
aiohttp>=3.13.3  # fix DoS: large payload (CVE-2025-69228 #29), assert bypass (CVE-2025-69227 #28), chunked messages (CVE-2025-69229 #30); also zip bomb; overrides langchain-community transitive dep
pydantic>=2.12.5
psutil>=7.2.2
brotli>=1.2.0  # ensure safe decompression (fixes DoS CVE); alternative: brotlicffi>=1.2.0
filelock>=3.25.2  # fix TOCTOU symlink vulnerabilities UnixFileLock (CVE-2025-68146 #33) and SoftFileLock (CVE-2026-22701)
langchain>=1.2.12  # core agent infrastructure (StructuredTool, LangGraph); includes recent security fixes via langchain-core
langchain-core>=1.2.19  # fix SSRF via image_url token counting CVE-2026-26013 (Dependabot alert #41); also covers prior serialization fix CVE-2025-68664
langchain-community>=0.4.1  # Note: has transitive dep on aiohttp 3.13.1, overridden above
langgraph>=1.1.2

# ===== API (FastAPI server features) =====
# fastapi>=0.135.1
# starlette>=0.52.1  # if using Starlette/FastAPI, ensure >=0.49.1 to fix Range header DoS (CVE-2025-62727)
# uvicorn>=0.42.0

# ===== TESTING (Development & Testing) =====
# pytest>=9.0.2
# pytest-mock>=3.15.1
# pytest-cov>=7.0.0
# pytest-asyncio>=1.3.0


# ===== OSINT (OSINT & Social Media Intelligence) =====
# phonenumbers>=9.0.26
# dnspython>=2.8.0
# tweepy>=4.16.0
# python-instagram>=1.3.2

# ===== LINKEDIN_API (Optional LinkedIn API - requires LinkedIn account) =====
# NOTE: linkedin-api is OPTIONAL. Web scraping (default) works without it.
# Installing this may pull lxml which can conflict with other packages.
# Only install if you need direct LinkedIn API access for OSINT.
# Requires LINKEDIN_EMAIL and LINKEDIN_PASSWORD environment variables.
# See docs/osint/SOCIAL_INTELLIGENCE.md for setup and ToS implications.
# linkedin-api==2.3.1
# lxml==6.0.2


# ===== LLM_OLLAMA (Local Ollama LLM) =====
## (langchain/langgraph moved to CORE - required by all providers)

# ===== LLM_OPENAI (OpenAI GPT models) =====
# openai>=2.29.0

# ===== LLM_ANTHROPIC (Anthropic Claude models) =====
# anthropic>=0.85.0

# ===== LLM_GROQ (Groq fast inference) =====
# groq>=1.1.1



# --- Security overrides (transitive dependencies) ---
# NOTE: marshmallow 4.2.0 creates dependency conflict with dataclasses-json (<4.0.0),
# but all imports work correctly. Security fix takes priority over dep resolver warning.
marshmallow==4.2.2  # fix DoS CVE-2025-68480 (patched in 4.1.2, Dependabot alert #22); also GHSA-428g-f7cq-pgp5
# dataclasses-json 0.6.7 declares marshmallow<4.0.0 but works with 4.2.0

protobuf>=7.34.0  # fix JSON recursion depth bypass CVE-2026-0994 (Dependabot alert #37); transitive via chromadb
langsmith>=0.7.18  # fix SSRF via tracing header injection CVE-2026-25528 (Dependabot alert #38); transitive via langchain/langgraph
pillow>=12.1.1  # fix OOB write loading PSD images CVE-2026-25990 (Dependabot alert #40); transitive via sentence-transformers
langgraph-checkpoint>=4.0.1  # fix BaseCache deserialization RCE (Dependabot alert #42); transitive via langchain and langgraph

orjson>=3.11.7  # fix recursion limit DoS in deeply nested JSON (Dependabot alert #44); transitive via chromadb, langchain*, langgraph
